import 'package:flutter/material.dart';

class PasscodePageView extends StatelessWidget {
  const PasscodePageView({super.key});

  @override
  Widget build(BuildContext context) {
    throw UnimplementedError();
  }
}
