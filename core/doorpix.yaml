on:
  startup:
    - log: hello world # prints hello world to the console 
    - eval:
        - echo "I am a shell command running in $SHELL"
  shutdown:
    - log: goodbye world # prints goodbye world to the console
